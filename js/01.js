// Variables let
// 1. var: ya casi no se utiliza en la sintaxis moderna
// 2. let
// 3. const javascript es de tipo dinamico el tip de dato se almacena en el valor de la variable y no en la variable como tal
// creas una variable como tipo string y despues le puedes asignar otros tipos de datos 
let client = "Juan";
let precioDescuento = 220; // CamelCase es la forma en la que se escriben las variables o funciones que tiene mas de una palabra


// Reasignar
client = 20
client = true

// Pueden iniciar sin un valor
let precio 
precio = 1000;

console.log(precio);
console.log(client);
console.log(precioDescuento);

